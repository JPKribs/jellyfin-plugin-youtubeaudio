---
name: "Server Sync"
guid: "ebd650b5-6f4c-4ccb-b10d-23dffb3a7286"
version: "10.11.27"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Sync media content from a source Jellyfin server to this server."
description: >
  Server Sync enables one-way content synchronization from a source Jellyfin server to a local server.
  The plugin periodically scans the source server for media items (Movies, Episodes, Audio, Video)
  and downloads missing or updated content. Features include library-to-library mapping with path
  translation, ETag-based change detection, configurable approval workflows, bandwidth throttling
  with time-based scheduling, and companion file support (subtitles, etc.).
category: "General"
owner: "JPKribs"
artifacts:
- "Jellyfin.Plugin.ServerSync.dll"
- "Jellyfin.Sdk.dll"
- "Microsoft.Kiota.Abstractions.dll"
- "Microsoft.Kiota.Http.HttpClientLibrary.dll"
- "Microsoft.Kiota.Serialization.Form.dll"
- "Microsoft.Kiota.Serialization.Json.dll"
- "Microsoft.Kiota.Serialization.Multipart.dll"
- "Microsoft.Kiota.Serialization.Text.dll"
- "Std.UriTemplate.dll"
changelog:
  new:
  - Enable Server Sync in the admin dashboard sidebar (EnableInMainMenu) for quick access alongside other server management pages
  - Add library-level ignored paths to skip entire folders from Content, Metadata, and History sync
  - Add wildcard support for ignored paths (e.g., "Star Wars*" matches all folders starting with that prefix)
  improvements:
  - Move database reset controls from sync tabs to Settings > Troubleshooting > Database for centralized management
  - Add individual table resets (Content, History, Metadata, User) alongside full database reset
  - Use native Jellyfin fonts and CSS for Troubleshooting section styling consistency
  - Modal change summary badges wrap to multiple rows on narrow/mobile screens
  fixes:
  - Fix pagination breaking when switching between sync tabs without refreshing the page
  - Fix queued/errored items not transitioning to synced when local file appears on subsequent refresh